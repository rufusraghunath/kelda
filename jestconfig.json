{
  "preset": "ts-jest",
  "testRegex": "test\\.ts?$",
  "moduleFileExtensions": ["ts", "js", "json", "node"],
  "setupFilesAfterEnv": ["<rootDir>/setupTests.ts"],
  "collectCoverageFrom": [
    "src/**/*.{ts,js}",
    "!src/index.ts",
    "!src/dom-mocks/**/*",
    "!src/util/testUtils.ts",
    "!src/**/*.d.ts",
    "!src/kelda/testScript.*.js"
  ],
  "coverageThreshold": {
    "global": {
      "branches": 85,
      "functions": 90,
      "lines": 90,
      "statements": 90
    }
  }
}
